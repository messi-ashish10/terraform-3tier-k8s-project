apiVersion: v1
kind: Service
metadata:
  name: mongo-service
  labels:
    app: mongo
    tier: database
    project: project1
spec:
  selector:
    app: mongo
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
  type: ClusterIP   # This is correct — Mongo should NOT be exposed externally
